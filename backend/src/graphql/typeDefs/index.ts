//This index file is going to serve as aplace where we can combine all of our typedefs from all of our indivdual entity files

import userTypeDefs from "./user";

const typeDefs = [userTypeDefs];

export default typeDefs;
